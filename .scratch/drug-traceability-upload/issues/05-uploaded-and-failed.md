# 05 — 已上传 & 失败记录

**What to build:** 已上传和失败记录两个子页面，复用上传任务页面的表格组件和交互模式。完成后可以查看所有上传历史和失败详情。

**Blocked by:** 04 — 上传任务页面

**Status:** ready-for-agent

- [ ] 已上传子页面表格：日期、单号、往来单位、追溯码、任务状态、API 返回详情
- [ ] API 返回详情列：显示为可展开/收起的格式化文本或弹窗查看
- [ ] 失败记录子页面表格：日期、单号、往来单位、追溯码、任务状态、API 返回详情
- [ ] 失败记录操作列：编辑、删除、手动重传、复选框（与上传任务页面一致）
- [ ] 失败记录全选 + 批量删除 + 批量重传
- [ ] 两个子页面都有搜索框和列筛选功能（与上传任务一致）
- [ ] 数据源从 `upload_logs` 表按 success 字段区分（1=已上传，0=失败）
- [ ] API 端点：`GET /api/uploaded`、`GET /api/failed`
- [ ] 重传时更新关联的 `upload_tasks` 状态和 `upload_logs` 记录
